import { Specimen, Editor } from 'hanga'
import Button from './Button.jsx'

# Button

This is a button that can be used for interactivity or linking.

## Props

<table>
<thead>
  <tr>
    <th>Prop</th>
    <th>Type</th>
    <th>Description</th>
    <th>Value</th>
  </tr>
</thead>
<tbody>
  <tr>
    <td><code>href</code></td>
    <td>String</td>
    <td>Optional href will use an `a` tag instead of a `button`</td>
    <td><Editor.Text hideLabel name="href" /></td>
  </tr>
  <tr>
    <td><code>block</code></td>
    <td>Bool</td>
    <td>Optional href will use an `a` tag instead of a `button`</td>
    <td><Editor.Toggle hideLabel name="block" /></td>
  </tr>
  <tr>
    <td><code>size</code></td>
    <td>Enum</td>
    <td></td>
    <td><Editor.Select hideLabel name="size" options={['small', 'medium', 'large']} defaultValue="medium" /></td>
  </tr>
  <tr>
    <td><code>theme</code></td>
    <td>Enum</td>
    <td></td>
    <td><Editor.Select hideLabel name="theme" options={['passive', 'positive', 'negative']} defaultValue="passive" /></td>
  </tr>
  <tr>
    <td><code>children</code></td>
    <td>Children</td>
    <td></td>
    <td><Editor.Text multiline hideLabel name="children" defaultValue="Aliqua tempor labore minim fugiat ipsum." /></td>
  </tr>
</tbody>
</table>

<Specimen
  Head={() => <link rel="stylesheet" href="/_next/static/style.css" />}
>
  {
    ({ href, block, size, theme, children }) =>
      <Button
        href={href}
        block={block}
        size={size}
        theme={theme}
      >
        {children}
      </Button>
  }
</Specimen>
